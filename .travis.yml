language: node_js
node_js:
  - "node"
before_script:
  - npm install -g tiddlywiki
script: tiddlywiki ./*wiki --listen host=nomieletnapr.github.io port=8080

after_success:
  - bash ./publish.sh

branches:
  only:
    - master
