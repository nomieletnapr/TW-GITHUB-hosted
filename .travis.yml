language: node_js
node_js:
  - "node"
before_script:
  - npm install -g tiddlywiki
script: 
  - tiddlywiki ./*wiki --listen &
  - sleep 10
  - ssh StrictHostKeyChecking=accept-new -R 80:127.0.0.1:8080 ssh.localhost.run
after_success:
  - bash ./publish.sh

branches:
  only:
    - master
